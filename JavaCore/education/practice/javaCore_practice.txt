Задание на понимание того каким образом передаются данные в Java

- Создать класс valueTransfer
- создать локальный класс или отдельно класс ValueTransferMethod
- создать локальный класс или отдельно класс ValueTransferObject
- В классе ValueTransferObject создать public поле типа Integer и типа int (значение 0 и 0)
- Объявить main в котором будут создаваться объекты ValueTransferMethod и ValueTransferObject
- Внутри main метода создать поле типа Integer и типа int (значение 0 и 0)
- В классе ValueTransferMethod объявить метода increment (возвращаемое значение void, аргументы (ValueTransferObject obj, Integer integer, int intt))
- Внутри метода increment произвели инкремент всех значений (2 полей объекта и 2 присылаемых значений)
- использовать increment
- использовать sout в который необходимо поместить (ValueTransferObject obj, Integer integer, int intt)
- Сделать вывод о том как передаются вещи в Java
Задание для понимания Абстрактных классов

1. Создать класс ВоенныйШтаб
2. В данном классе объявить 2 метода, абстрактный метод возвращающий String с название ВычислениеМестоположения
и Обычный метод c название Отчет который имеет следующую реализацию
...
{
sout("//Вычисление местоположение" + ВычислениеМестоположения())
}
...
3. Создать класс ПодразделениеПехоты который будет наследоваться от класса ВоенныйШтаб.
Добавить поле String name которое будет задаваться строго через конструктор
Переопределить необходимые методы так чтобы ВычислениеМестоположения возвращал строку из радомизированных значений формата "Name - X,Y"
Добавить поле String name которое будет задаваться строго через конструктор
4. Создать класс ЛетноеПодразделение который будет наследоваться от класса ВоенныйШтаб.
Переопределить необходимые методы так чтобы ВычислениеМестоположения возвращал строку из радомизированных значений формата "Name - X,Y,Z"
5. Создать класс main в методе которого будет создаваться лист, который будет содержать
в себе ПО 5 объектов класса ПодразделениеПехоты и ЛетноеПодразделение
(Обратить внимание на тип листа в случае затупа)
6. Воспользовавшись циклом foreach задействовать метод Отчет у каждого созданного объекта.
7. Посмотреть на результат и сделать выводы.